import { styled } from '../../styles/Theme.provider';

export const ToastContainer = styled('div', {
  position: 'fixed',
  top: '0',
  right: '0',

  width: '100%',
  padding: '10px',
  maxWidth: '300px',
});
